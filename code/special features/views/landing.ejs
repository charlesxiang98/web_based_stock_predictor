<!DOCTYPE html>
<html>
        <head>
            <title>🚀 Rocket</title>
            <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
            <link rel="stylesheet" href="/stylesheets/landing.css">
            <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js" type="text/javascript" async></script>
        </head>
        <body>
            <div class="content">
                <div class="container">
                    <% if(error && error.length > 0){ %>
                        <div class="alert alert-danger" role="alert">
                            <%= error %>
                        </div>
                    <% } %>
                    <% if(success && success.length > 0){ %>
                        <div class="alert alert-success" role="alert">
                            <%= success %>
                        </div>
                    <% } %>
                </div>
        
                <div id="landing-header">
                    <h1>🚀 Rocket</h1>
                    <h3 style="color: white">Rutgers stOCK forEcasTer</h3>
                    <p>
                        <form action="/courses" method="GET" class="form-inline">
                            <div form-group>
                                <input type="text" name="search" placeholder="Input Stock Symbol" class="form-control"></input>
                                <input type="submit" value="Search" class="btn btn-primary"></input>
                            </div>
                        <form>
                    </p>
                    <a href="/courses" class="btn btn-lg btn-primary">Get Rich Now</a>
                </div>
        
                <ul class="slideshow">
                  <li></li>
                  <li></li>
                  <li></li>
                  <li></li>
                  <li></li>
                </ul>

<% include partials/footer %>